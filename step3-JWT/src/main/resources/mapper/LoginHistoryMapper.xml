<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.auth.mapper.LoginHistoryMapper">
    <!-- 로그인 기록 -->
    <insert id="insert">
        INSERT INTO login_history (
            user_no, ip_address, user_agent, success
        )
        VALUES (
            #{userNo}, #{ip}, #{agent}, #{success}
        )
    </insert>
    <!-- 로그인 기록 조회 -->
    <select id="findByUserNo" resultType="LoginHistory">
        SELECT 
            no,
            user_no,
            ip_address,
            user_agent,
            success,
            created_at
        FROM login_history
        WHERE user_no = #{userNo}
        ORDER BY no DESC
    </select>
</mapper>